[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  PNPM_VERSION = "8.14.2"

# Next.js Runtime handles routing automatically
# API routes at /api/* are automatically converted to serverless functions

